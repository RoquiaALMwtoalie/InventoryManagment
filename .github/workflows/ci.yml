name: Build and Deploy Electron App

on:
  push:
    branches:
      - main  # أو أي فرع آخر ترغب في تتبع التغييرات عليه
  pull_request:
    branches:
      - main  # أو أي فرع آخر ترغب في تتبع التغييرات عليه

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Install dependencies
        run: |
          cd $GITHUB_WORKSPACE  # هذا سيأخذك إلى المسار الجذري للمشروع
          yarn install
      - name: Build Electron app
        run: |
          cd $GITHUB_WORKSPACE  # التعديل هنا أيضاً
          yarn electron:mac
